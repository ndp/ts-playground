# JavaScript Support

lit-md works with JavaScript files too.
Code blocks render with `js` syntax highlighting instead of `ts`.
## Basic Example

```js
const msg = 'hello'
msg.length // => 5
```

## Functions Work Too

```js
const sum = () => 1 + 2
sum() // => 3
```
