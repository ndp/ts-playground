{
  "name": "@ndp-software/world",
  "version": "0.1.0",
  "private": true,
  "description": "world",
  "license": "ISC",
  "type": "module",
  "files": [
  ],
  "sideEffects": false,
  "scripts": {
    "build": "npm run clean && npm run build:js && npm run build:types",
    "build:js": "esbuild --outdir=./build/ --public-path=/ts-playground/build/ ./*.ts",
    "build:js:watch": "npm run build:js -- --watch",
    "typecheck": "tsc -p config/tsconfig.check.json --noEmit",
    "typecheck:watch": "tsc -p config/tsconfig.check.json --noEmit --watch",
    "test": "node --test -r ../../test/setup-jsdom.js './**/*test.ts'",
    "test:watch": "node --test --watch -r ../../test/setup-jsdom.js './**/*test.ts'",
    "dev": "sh -c 'npm run test:watch & npm run typecheck:watch & npm run build:js:watch & wait'",
    "verify": "npm run typecheck && npm run test",
    "clean": "rimraf ./build"
  }
}
