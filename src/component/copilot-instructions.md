See ../../.github/copilot-instructions.md for instructions on how to use Copilot to contribute to this component.

## Validation:

- Run component verification (typecheck + tests):

	`npm --prefix ./src/component run verify`

- Build component library artifact + declarations:

	`npm --prefix ./src/component run build`
